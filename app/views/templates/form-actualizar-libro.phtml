<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow border-info">

                <div class="card-header bg-info text-dark">
                    <h1 class="h4 mb-0">Actualizar Libro</h1>
                </div>

                <div class="card-body">

                    <p class="text-muted small">
                        Seleccione un libro y modifique los campos necesarios.
                    </p>

                    <form action="actualizarLibro" method="post">

                        <div class="mb-4">
                            <label class="form-label fw-bold">Libro a editar</label>

                            <select name="id_libro" class="form-select border-info" required>
                                <option value="" selected disabled>
                                    Seleccione un libro...
                                </option>

                                <?php foreach ($libros as $libro) : ?>
                                    <option value="<?= $libro->id_libro ?>">
                                        <?= $libro->titulo ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="form-label fw-bold">Título del Libro</label>

                                <input type="text"
                                       name="titulo"
                                       class="form-control border-info"
                                       placeholder="Editar título..."
                                       required>
                            </div>

                            <div class="col-md-4">
                                <label class="form-label fw-bold">Año de publicación</label>

                                <input type="number"
                                       name="anio"
                                       class="form-control border-info"
                                       placeholder="YYYY"
                                       required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Sinopsis</label>

                            <textarea name="sinopsis"
                                      class="form-control border-info"
                                      rows="3"
                                      required></textarea>
                        </div>

                        <div class="row mb-3 align-items-end">

                            <div class="col-md-6">
                                <label class="form-label fw-bold">Autor</label>

                                <select name="autor" class="form-select border-info" required>
                                    <option value="" selected disabled>
                                        Seleccione el autor...
                                    </option>

                                    <?php foreach ($autores as $autor) : ?>
                                        <option value="<?= $autor->id_autor ?>">
                                            <?= $autor->nombre ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>

                            <div class="col-md-6">
                                <label class="form-label fw-bold">URL de la tapa</label>

                                <input type="text"
                                       name="tapa"
                                       class="form-control border-info">
                            </div>

                        </div>

                        <div class="mb-4">
                            <div class="form-check form-switch">

                                <input class="form-check-input"
                                       type="checkbox"
                                       name="disponible"
                                       id="actualizarDisponible"
                                       checked>

                                <label class="form-check-label" for="actualizarDisponible">
                                    Marcar como disponible
                                </label>

                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-info btn-lg">
                                Actualizar Datos
                            </button>
                        </div>

                    </form>

                </div>
            </div>

        </div>
    </div>
</div>
