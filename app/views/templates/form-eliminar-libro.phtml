<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white">
                    <h1 class="h4 mb-0">Eliminar Libro</h1>
                </div>
                <div class="card-body">
                    <p class="text-muted">Seleccione el libro que desea quitar permanentemente de la biblioteca.</p>

                    <form action="eliminarLibro" method="post">
                        <div class="mb-4">
                            <label class="form-label fw-bold">Libro a eliminar</label>
                            <select name="libroAEliminar" class="form-select border-danger" required>
                                <option value="" selected disabled>Elija un título...</option>
                                <?php foreach ($libros as $libro) : ?>
                                    <?php var_dump($libro); ?>

                                    <option value="<?php echo $libro->id_libro ?>">
                                        <?php echo $libro->titulo ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger btn-lg">
                                Confirmar Eliminación
                            </button>
                            <a href="home" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="alert alert-warning mt-3" role="alert">
                <strong>Atención:</strong> Esta acción no se puede deshacer.
            </div>
        </div>
    </div>
</div>
