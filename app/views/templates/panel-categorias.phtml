<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h1 class="h4 mb-0">Agregar Autor</h1>
                </div>
                <div class="card-body">
                    <form action="agregarAutor" method="post">

                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="form-label">Nombre del Autor</label>
                                <input type="text" name="nombre" class="form-control" placeholder="Ej: Stephen King" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Fecha de nacimiento</label>
                                <input type="date" name="fechaDeNacimiento" class="form-control" placeholder="YYYY" required>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="form-label">Nacionalidad</label>
                                <input type="text" name="nacionalidad" class="form-control" placeholder="Ej: Argentina" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Biografia</label>
                                <textarea name="biografia" class="form-control" rows="3" placeholder="Breve resumen del autor..." required></textarea>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-success btn-lg">Guardar Autor</button>
                            </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>






<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white">
                    <h1 class="h4 mb-0">Eliminar Autor</h1>
                </div>
                <div class="card-body">
                    <p class="text-muted">Seleccione el autor que desea quitar permanentemente de la biblioteca.</p>

                    <form action="eliminarAutor" method="post">
                        <div class="mb-4">
                            <label class="form-label fw-bold">Autor a eliminar</label>
                            <select name="autorAEliminar" class="form-select border-danger" required>
                                <option value="" selected disabled>Elija un autor...</option>
                                <?php foreach ($autores as $autor) : ?>
                                    <?php var_dump($autor); ?>

                                    <option value="<?php echo $autor->id_autor ?>">
                                        <?php echo $autor->nombre ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger btn-lg">
                                Confirmar Eliminación
                            </button>
                            <a href="home" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="alert alert-warning mt-3" role="alert">
                <strong>Atención:</strong> Esta acción no se puede deshacer.
            </div>
        </div>
    </div>
</div>





<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow border-info">

                <div class="card-header bg-info text-dark">
                    <h1 class="h4 mb-0">Actualizar Autor</h1>
                </div>

                <div class="card-body">

                    <p class="text-muted small">
                        Seleccione un autor y modifique los campos necesarios.
                    </p>

                    <form action="<?= BASE_URL ?>actualizarAutor" method="post">

                        <div class="mb-4">
                            <label class="form-label fw-bold">Autor a editar</label>

                            <select name="id_autor" class="form-select border-info" required>
                                <option value="" selected disabled>
                                    Seleccione un autor...
                                </option>

                                <?php foreach ($autores as $autor) : ?>
                                    <option value="<?= $autor->id_autor ?>">
                                        <?= $autor->nombre ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="form-label fw-bold">Nombre del Autor</label>

                                <input type="text"
                                    name="nombre"
                                    class="form-control border-info"
                                    placeholder="Editar nombre..."
                                    required>
                            </div>

                            <div class="col-md-4">
                                <label class="form-label fw-bold">Fecha de nacimiento</label>

                                <input type="date"
                                    name="fechaDeNacimiento"
                                    class="form-control border-info"
                                    placeholder="YYYY"
                                    required>
                            </div>
                        </div>

                        <div class="row mb-3">

                            <div class="col-md-8">
                                <label class="form-label fw-bold">Nacionalidad</label>

                                <input type="text"
                                    name="nacionalidad"
                                    class="form-control border-info"
                                    placeholder="Editar nacionalidad..."
                                    required>
                            </div>

                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Biografia</label>

                            <textarea name="biografia"
                                class="form-control border-info"
                                rows="3"
                                required></textarea>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-info btn-lg">
                                Actualizar Datos
                            </button>
                        </div>

                    </form>

                </div>
            </div>

        </div>
    </div>
</div>